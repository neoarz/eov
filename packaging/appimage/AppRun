#!/bin/sh
set -eu

HERE="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"

if [ -d "$HERE/usr/lib" ]; then
    if [ -n "${LD_LIBRARY_PATH:-}" ]; then
        export LD_LIBRARY_PATH="$HERE/usr/lib:$LD_LIBRARY_PATH"
    else
        export LD_LIBRARY_PATH="$HERE/usr/lib"
    fi
fi

exec "$HERE/usr/bin/eov" "$@"
