[package]
name = "grayscale_plugin"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
abi_stable = { version = "0.11", default-features = false }
ash = "0.38"
plugin_api = { path = "../../plugin_api" }
